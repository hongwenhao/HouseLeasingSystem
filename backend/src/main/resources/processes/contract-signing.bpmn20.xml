<?xml version="1.0" encoding="UTF-8"?>
<definitions xmlns="http://www.omg.org/spec/BPMN/20100524/MODEL"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:activiti="http://activiti.org/bpmn"
             xmlns:bpmndi="http://www.omg.org/spec/BPMN/20100524/DI"
             xmlns:omgdc="http://www.omg.org/spec/DD/20100524/DC"
             xmlns:omgdi="http://www.omg.org/spec/DD/20100524/DI"
             typeLanguage="http://www.w3.org/2001/XMLSchema"
             expressionLanguage="http://www.w3.org/1999/XPath"
             targetNamespace="http://www.activiti.org/test">

    <!--
      合同签署主流程（顺序签署）：
      1) 租客先签（tenantSignTask）；
      2) 房东后签（landlordSignTask）；
      3) 双方节点均完成后流程结束（endEvent）。
      该流程用于驱动业务层“合同状态从 DRAFT 到 FULLY_SIGNED”的状态推进。
    -->
    <process id="contractSigningProcess" name="合同签署流程" isExecutable="true">

        <startEvent id="startEvent" name="开始"/>

        <!-- 租客签署前置：流程启动后首先进入租客任务，保证“承租方先确认合同” -->
        <sequenceFlow id="flow1" sourceRef="startEvent" targetRef="tenantSignTask"/>

        <userTask id="tenantSignTask" name="租客签署"
                  activiti:assignee="${tenantId}">
            <extensionElements>
                <!-- tenantSigned 由业务服务在 complete(task, vars) 时写入，用于记录租客签署结果 -->
                <activiti:formProperty id="tenantSigned" name="租客已签署" type="boolean"/>
                <!-- contractId 贯穿流程实例，便于任务节点与业务合同主键做稳定关联 -->
                <activiti:formProperty id="contractId" name="合同ID" type="long"/>
            </extensionElements>
        </userTask>

        <!-- 租客签署完成后，进入房东签署节点 -->
        <sequenceFlow id="flow2" sourceRef="tenantSignTask" targetRef="landlordSignTask"/>

        <userTask id="landlordSignTask" name="房东签署"
                  activiti:assignee="${landlordId}">
            <extensionElements>
                <!-- landlordSigned 记录出租方签署结果，供业务层判断是否可进入 FULLY_SIGNED -->
                <activiti:formProperty id="landlordSigned" name="房东已签署" type="boolean"/>
                <activiti:formProperty id="contractId" name="合同ID" type="long"/>
            </extensionElements>
        </userTask>

        <!-- 双方签署任务完成后流程结束，业务层可据此触发合同签署完成后续动作 -->
        <sequenceFlow id="flow3" sourceRef="landlordSignTask" targetRef="endEvent"/>

        <endEvent id="endEvent" name="合同签署完成"/>

    </process>

</definitions>
